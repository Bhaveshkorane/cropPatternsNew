1.Activate the environment present At 

2. start the server 
python manage.py runserver

3. in second terminal with same environment  for startning the worker
celery -A crop_patterns worker --loglevel=info 
